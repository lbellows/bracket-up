Much smaller download. The app now ships a separate build for each device
architecture instead of one bundle containing all of them, and the compiled
code is minified. Nothing about the app itself has changed.
